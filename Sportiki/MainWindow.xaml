<Window x:Class="Sportiki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sportiki"
        xmlns:models="clr-namespace:Sportiki.DB"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="500"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Добавить спортсмена" Command="{Binding AddAthleteCommand}" Width="150" Margin="5"/>
            <Button Content="Добавить тренировку" Command="{Binding AddTrainingCommand}" Width="150" Margin="5"/>
            <Button Content="Добавить оценку" Command="{Binding AddProgressCommand}" Width="120" Margin="5"
                    IsEnabled="{Binding CanAddProgress}"/>
            <Button Content="Удалить тренировку" Command="{Binding DeleteTrainingCommand}" Width="150" Margin="5"/>
            <Button Content="Обновить" Click="Update_click" Width="80" Margin="5"/>
            <Button Content="Сохранить" Click="Save_click" Width="80" Margin="5"/>
            
        </StackPanel>
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Спортсмены" FontWeight="Bold" FontSize="14" Margin="0 0 0 10"/>
                <ListBox ItemsSource="{Binding Athletes}" SelectedItem="{Binding SelectedAthlete}"
                         DisplayMemberPath="FullName" Height="200"/>

                <TextBlock Text="Тренировки" FontWeight="Bold" FontSize="14" Margin="0 20 0 10"/>
                <ListBox ItemsSource="{Binding Trainings}" SelectedItem="{Binding SelectedTraining}"
                         DisplayMemberPath="Title" Height="200"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Оценки и комментарии" FontWeight="Bold" FontSize="14" Margin="0 0 0 10"/>
                <DataGrid ItemsSource="{Binding ProgressList}" AutoGenerateColumns="False" Height="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Спортсмен ID" Binding="{Binding AthleteId}" Width="80"/>
                        <DataGridTextColumn Header="Тренировка ID" Binding="{Binding TrainingId}" Width="80"/>
                        <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="60"/>
                        <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
